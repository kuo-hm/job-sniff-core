syntax = "proto3";

package scraper;

service ScraperService {
  rpc StartScrape (ScrapeRequest) returns (ScrapeResponse);
}

message ScrapeRequest {
  string userId = 1;
  string keyword = 2;
  repeated string sites = 3;
}

message ScrapeResponse {
  bool success = 1;
  string jobId = 2;
}
